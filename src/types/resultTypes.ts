export type statusArray = {
    title: string;
    image: string;
    leftText: string;
    leftPercent: string;
    rightText: string;
    rightPercent: string;
    text: string;
}

export type result = {
    title: string;
    image: string;
    text: string;
}